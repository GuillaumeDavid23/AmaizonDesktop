import React from "react";

import Box from "@mui/material/Box";
import Typography from "@mui/material/Typography";

const CustomerListItem = () => {
    return (
        <Box>
            <Typography>CustomerListItem</Typography>
        </Box>
    );
};

export default CustomerListItem;
